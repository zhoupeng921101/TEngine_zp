# plan 状态

当前任务:排行榜结算客户端段·本地结算退役 + 红点交棒邮件(全栈三刀收口·客户端段)

设计稿改动:
- 改写 `design-docs/22-rank-system.md`(整篇过时段重写 / 删除,不加勘误注,conventions §6):
  - 篇首两段引子(「本设计建一套排名数据逻辑层」段 + 「因为本作离线」段)改写:客户端层不再含「到点结算 / 按名次档查奖励 / 发结算邮件」叙述,改为「结算上移服务端,客户端只查榜+每日点赞+红点」。
  - §读前必看 6 条第 2/3/4 条改写:① 结算编排上移服务端、客户端不本地结算;② 持久化去掉「上次结算时间 / 已结标记」(权威移服务端);③ 「结算时机由注入时钟驱动」改为「每日 / 点赞跨天由注入日期驱动」(结算时机移服务端时钟)。
  - §立项信息:类型 / 方向约束 / 影响范围 / 关键约束逐项改写,标明「结算编排上移服务端,本设计客户端层不再含」。
  - §一表(15 行)改:行 4(rank_min/max)、行 8(valid_type/val)、行 9(mail)三行改为「服务端权威,客户端只承载不消费」;末段「不做」清单不变(原本就含真实全服排名)。
  - §二「数据模型与分层」mermaid 改:服务层不再含「结算编排」节点;持久化节点不再含「上次结算 / 已结算」;新增「设计 33 服务端」子图标明结算 / 名次档奖 / 周期幂等。
  - §3.1 配置表:valid_val / mail 两行描述加「客户端不消费、服务端消费」;示例行末句「奖励 / 邮件模板缺失结算仍发」改指设计 33。
  - §3.2 运行期榜定义:「按名次查中奖档」加「客户端用于 UI 奖励预览;实际发奖由设计 33 服务端裁定」;末句「供 UI 分页 / 登录时遍历检查结算」改为「供 UI 分页」。
  - §3.3 排名服务持有项:把「时钟」改成「日期(每日点赞跨天用)」。
  - **§3.5 整段改写**(锚 `#settle` / `#due` / `#orchestrate` 全保留供入站):标题改「结算编排:服务端权威」;3.5.1 改「结算时机 + 名次档奖 + 邮件模板的配置语义保留」(配置仍在,消费方变);3.5.2 改「客户端不参与结算编排」+「断服不本地补结算」旁注(防双发)。
  - **§3.7 红点重写**(锚 `#reddot` 保留):红点 = 每日 + 点赞;结算奖红点交邮件红点 21。判定从 3 条降为 2 条(去「到点未结算」)。
  - §3.8 持久化:删除「上次结算时间 / 已结算周期标记」字段,DTO 字段表退化(本机最佳 + 达到时间 + 每日 / 点赞领取日期)。
  - §3.9 文案占位:锚 `#text` + 8 个 textId 占位常量**全保留**(设计 33 仍引用)。
  - §四 时序图整段重写:删除「B · 结算」流,改为「A · 查榜显示 / B · 每日 + 点赞奖领取」两条;末注指向设计 32 邮箱领结算奖。
  - §五 验收点重写:删除「结算时机 DUE」整组 4 条 + 「结算编排 ST」整组 3 条;RD2 删除「结算后该榜不再触发红点」断言;P1 删除「上次结算时间往返」断言;**新增「退役 RT」组 3 条**(RT1 = 无结算公共方法 / 字段、RT2 = HUD 入口红点不再因到点亮、RT3 = 既有 RankSystemTests 中 DUE/ST 整组删除 + RD 去结算分支 + P1 改字段);P2 加「老存档含遗留结算字段不抛」;R2 Code Review 重点核条改为「客户端本地结算退役 / 红点交邮件 21 / DTO 退化」。
  - §六 待拍板 O5 改「每日 / 点赞经邮件 21、结算经设计 33 服务端」;O9 改「不在客户端,结算上移服务端;客户端无结算触发入口」。
  - §七 风险表删除「另造发奖逻辑(原是结算)」「结算重复发奖」「未入榜 / 无档 结算崩」3 条;新增「客户端本地补结算 → 双发」「客户端持上次结算时间 → 跨端不一致」「老存档含遗留字段」3 条;「红点与邮件红点重复亮」改写为「本设计若仍保留到点未结分支则与邮件红点指同一封」。
- 改写 `design-docs/33-rank-settle-server.md`(本任务联动同步):
  - 读前必看第 6 条「本刀不动客户端 / 不动设计 22 正文」改为「客户端段已落地:设计 22 §3.5 / §3.7 / §3.8 已改写」。
  - §六 整段重写(锚 `#seam` 保留):标题改「与设计 22 的关系(结算上移 + 客户端段已落地)」;关系表 7 行写「退役前 / 退役后」对照(原是「设计 22 现状 / 客户端段适配前瞻」),加「断服不本地补结算」一行;末段「全栈三刀已全部落地」总结。
  - §九 风险表「误改设计 22 客户端正文」过时 → 改为「客户端段误本地补结算 → 双发」(对称设计 22 §3.5.2 旁注)。
- 同步 `design-docs/28-rank-window-art.md`(他篇引用客户端结算 API):
  - §七 处置表「红点」加「仅每日 / 点赞,结算移服务端见设计 33」;「结算触发」整行重写「不触发(结算已移服务端)」。
  - §十 D4 行重写「不触发(结算上移服务端,客户端无结算入口)/ 备选 = 退役无备选」;D6 加「每日 / 点赞;结算红点交邮件红点 21」。
- 同步 `design-docs/32-mail-server.md`(他篇引用设计 22 IsSettleDue):
  - §五崩法表「发奖入口(服务端进程内)」行的「未来排行榜结算 / 设计 22 §3.4 IsSettleDue 幂等防重复结」改为「设计 33 服务端结算 / 设计 33 §3.3 结算幂等」。
  - §九风险表「发奖入口被误当结算幂等保证」行同步改指设计 33。
- nav.js / index.html 无改动(不新增文档,不改 anchor 不漂)。
- 自检:
  - 22 / 33 改写未破坏锚 id:`#settle` `#due` `#orchestrate` `#reddot` `#persist` `#text` `#seam` 全部保留,被入站引用的章节号 §3.5 / §3.7 / §3.8 / §六 编号未移位(删整段会移位故采「整段改写」方式保住编号)。
  - code-blind:22 改写后未出现代码符号 / 方法名 / 类名(`RankService` `CheckAndSettle` `HasClaimable` 等)在 design-docs 正文,这些归交接区给 dev;28-rank-window 已有的 `HasClaimable` / `ClaimPraise` 等是 UI 实现接线稿,28 本身是实现文档(conventions §3 专项例外),沿用不删。
  - 拟人比喻 grep:已自检无「钉死 / 焊死 / 绑死 / 打死 / 死在」。

## 本增量范围(给 dev / test 的硬边界)

- **客户端段任务范围**(对应设计 33 §8.2 CV1-CV5):
  - 设计 22 客户端本地结算编排退役:`RankService` 移除「结算检查 / 结算时机判定 / 上次结算时间 / 已结标记」相关公共表面(方法 / 字段 / 属性);`RankPersistence` DTO 同步移除「上次结算时间 / 已结标记」字段。
  - 红点查询去结算分支:`HasClaimable` 简化为「每日可领 + 点赞可领」二判,不再包含「到点未结算」。
  - 既有 EditMode 测试同步改造(见 RT3 / R1):删除 DUE / ST 两组、RD 去结算分支断言、P1 改字段。
  - 排行榜入口红点(Tarot HUD)行为变化:不再因结算到点亮起(只看每日 / 点赞)——`RankWindow` 文件内只接红点查询的入口由该窗的入口侧调用,本任务不动该入口本身(28 / HUD 入口接线属表现层,设计已写)。
- **守住的不变量**:
  - 上报 RPC 通路与查榜远程源(设计 31 CV1-CV5)**不动**(上报 / 查榜均已 PASS,本任务零回归)。
  - 查榜断服回退本地源(本机 + 陪榜)**不动**:`GetBoardAsync` 远程失败回退 `GetBoard` 本地链路保留;`SubmitScoreAsync` 始终同步更新本地最佳保留。
  - 每日 / 点赞领取(`ClaimDaily` / `ClaimPraise`)**不动**:仍走 `IMailService.Send` 本机收件接口,跨天判定 `NowProvider().Date` 保留(`NowProvider` 字段本身保留,只删 `OpenDate` 字段)。
  - 本机最佳分(`SubmitScore` / `GetMyBest`)**不动**:仍是 selfProvider 链路 + 远程 RPC 双轨同步。
  - 邮件系统 21 / 设计 32 服务端段 **零改动**;无 proto 改动(本任务无新增 / 删除协议)。
- **不做**:
  - 服务端任何改动(设计 33 服务端段已 PASS / 2026-06-19 提交 420272b8)。
  - 任何新增 RPC 协议(本任务对协议层零改动)。
  - 真实「客户端段领结算邮件」实现(走设计 32 客户端段已落地的领取链,本任务无需再接)。
  - HUD / 主菜单的排行榜入口红点接线本身(28 已有,本任务只让红点取值不再包含结算分支即可)。

## 已拍板决策(decisions,自治默认推进,要改另开增量)

- **客户端 DTO 不保留遗留字段**:`RankBoardProgress` 删除 `lastSettleTicks` 字段。理由:① 字段无人读无人写后保留 = 死代码,违 conventions §6「过时即删」;② JsonUtility 反序列化对老存档遗留字段会忽略,旧设备升级后老存档加载不抛(P2 验收)、忽略遗留值即可,无升级路径风险;③ 设计 22 §3.8 已说明「上次结算时间 / 已结标记不再进客户端存档」,DTO 与正文一致更易后续维护。
- **`OpenDate` 字段同步删除**:RankService 的 `OpenDate` 仅服务 `OpenDays` 类结算判定;结算移服务端后,客户端无任何路径再读该字段。删除而非留作死字段(同上理由)。`NowProvider` 字段**保留**(`ClaimDaily` / `ClaimPraise` 跨天判定 / `SubmitScore` 写 bestAchievedTicks 仍需)。
- **公共表面退役清单**(给 dev 实现时的行为级清单,不写代码符号细节):RankService 不再对外暴露——① 「给定 now、open、lastSettle,判某榜是否到结算点」的判定方法;② 「检查所有榜并结算到点未结的榜,返本次结算了哪些榜」的结算编排方法;③ 「开服日期」字段;④ 「结算结果对象」类型本身亦无外用,可删除(无人读)。`SettleResult` struct 删除。
- **`HasClaimable` 简化判定**:从「到点未结 OR 每日可领 OR 点赞可领」三判 → 「每日可领 OR 点赞可领」二判;`IsSettleDue` 内部辅助一并删除(无外部调用)。
- **测试同步改造清单**(给 dev 落地参考,不替 dev 写测试):`RankSystemTests` 删除以下区块——DUE1/DUE2/DUE3/DUE4(结算时机四档)、ST1/ST2/ST3(结算编排)、RD2(结算后该榜不再触发红点);RD1 改写为「每日 / 点赞可领时亮、全领过不亮」(去掉「周榜到点未结 → true」断言);P1 跨实例往返断言改为「本机最佳 / 每日 / 点赞日期」(去掉「上次结算时间」断言)。`RankWindowTests` 中如有「`OpenDate` / `CheckAndSettle`」依赖项同步退役(grep 显示 L62/L63 是 `NowProvider`/`OpenDate` 注入——`OpenDate` 注入行删除,`NowProvider` 保留)。
- **服务构造签名保持**:`RankService` 构造 / GameContext 持有 / 注入入口(`InitRankWithDeps` 等)不改签名——结算移除是公共表面收敛,不动构造与注入接缝,对 28 RankWindow / GameContext.cs 调用点零改动(零回归友好)。
- **设计稿 anchor 编号保护**:22 §3.5 / §3.7 / §3.8 / §3.9 章节号未移位(改写而非删除整段,保住「设计 22 §3.5」「§3.7」类入站引用的可见编号 + 锚 id 双口径),33 / 28 / 32 等他篇的链接 `#22-rank-system::settle` / `::due` / `::orchestrate` / `::reddot` / `::text` / `::persist` 均仍可解析。
- **conventions §6 同步范围已审计**:本次改写过时的他篇 = 33 §六 / 33 读前必看第 6 条 / 28 §七 / §十 / 32 §五 / §九 → 全在本任务同步完成,无漂移窗口。

## 验收标准(client-test 逐条核对;完成定义=行为可观测,不写代码定位)

### A. 客户端段(client-test EditMode + 编译,本增量主验)

- **CV1·本地结算退役公共表面**:`RankService` 类对外不再有「结算检查(传入 now)/ 结算时机判定(传 def / now / open / lastSettle)/ 开服日期字段 / 结算结果对象」相关公共方法、字段、属性、类型;尝试访问应编译失败(已删除);`HasClaimable` 不再包含「到点未结算」判据(grep 实现内部不再调用「结算时机判定」)
- **CV2·DTO 字段退化**:`RankBoardProgress` 持久化 DTO 不再含「上次结算时间 / 已结标记」字段;`RankProgressSave` 加载老存档(JSON 内含遗留 `lastSettleTicks` 字段)反序列化**不抛、忽略遗留值**(JsonUtility 默认行为);新建服务实例往返存档,本机最佳分 + 每日 / 点赞领取日期保真
- **CV3·红点查询行为**:`HasClaimable` 在「周榜到点未结 + 无每日 / 点赞可领」场景下返 false(老断言改为反向);「有每日 / 点赞可领」场景仍返 true;领过当天每日 / 点赞后该项不再触发红点
- **CV4·查榜远程 / 本地不受影响**:`GetBoard` / `GetBoardAsync` / `GetMyRank` 行为零改变(排序 / 入榜 / 上限 / 我的名次 / 远程短路本地排序 / 断服回退本地源);`SubmitScore` / `SubmitScoreAsync` 行为零改变(本地最佳更新 + 远程取最优裁决)
- **CV5·每日 / 点赞领取不受影响**:`ClaimDaily` / `ClaimPraise` 行为零改变(经 `IMailService.Send` 投本机邮件、跨天判定经 `NowProvider().Date`、未入榜 / 无奖 / 今日已领文案 textId);邮件 21 接口零改动
- **CV6·测试同步**:既有 `RankSystemTests` 中 DUE / ST 两整组(共 7 个测试)删除;RD1 改写为「每日 / 点赞」二判;RD2 删除「结算后该榜不再触发红点」断言;P1 删除「上次结算时间往返」断言。`RankWindowTests` 中 `OpenDate` 注入相关行删除。其余测试(SO / DP / P2 / SK 等)保留 + 全绿
- **CV7·编译 + 零回归**:热更程序集编译 0 error;除 CV6 同步删除的测试外,既有 EditMode 测试全绿(尤其设计 14 存档 / 21 邮件 / 31 远程源 / 32 邮件服务端段已通过的客户端测试不受影响)
- **CV8·HUD 入口红点表现**:`RankWindow.Svc.HasClaimable` 在示例:周榜 valid_type=3 / val=1(周一结算)/ 周二 / 上次结算=null / 本机当前名次空 / 每日 / 点赞均未领 → 老行为返 true(到点未结)、新行为返 false(不感知结算时机)
- **CV9·Code Review 5 红线**:① 异步优先(本任务无新异步入口,删除 `CheckAndSettle` 同步方法不引入新阻塞 IO);② 模块访问规范(`RankService` 不引设计 33 服务端 / 不引 Fantasy.Net,客户端仍可纯逻辑单测);③ 资源释放(本任务无资源加载,无新增 dispose 点);④ 热更边界(改动全在 `GameLogic/Module/Rank/` 三个文件 + `Editor/Tests/BlockBlast/RankSystemTests.cs` + `RankWindowTests.cs` 退役行,不动 GameProto / 不动框架);⑤ 事件解耦(本系统无事件,改动不引新事件)。**重点核**:「客户端不再持任何结算决策方法」(grep `IsSettleDue` `CheckAndSettle` `OpenDate` `SettleResult` `lastSettleTicks` 应全部无命中)+「`HasClaimable` 判定体不再含结算分支」+「DTO 字段表与设计 22 §3.8 一致」

### B. 服务端段(本次零改动 / 不验收)

- 设计 33 服务端段已 PASS(2026-06-19 提交 420272b8),本任务不触服务端代码;**验证手段** = 不动 fantasy-net 工程(grep diff 仅在客户端 UnityProject 内即视为已守住边界)

### C. 联调段(本次不强制验)

- 真往返:上报多账号成绩(设计 31 客户端 + 服务端段已通)→ 服务端到结算时机自动结算 → 上榜账号客户端拉邮件列表见结算邮件 → 领取得名次档奖 + 本地发奖展示
- 本任务客户端段不再含本地结算路径,联调验证的「结算」由服务端段已 PASS 兜底;真实联调依赖 MongoDB + 服务端可达,不可达列 **BLOCKED 非 FAIL**
- E4 协议同步:**无新增 / 删除协议**(本任务对 proto 零改动,E4 = N/A)

### 不验收 / BLOCKED

- 真实联调多账号 / 跨设备真实结算路径(依赖 MongoDB 可达 + 服务端段 + 多账号客户端可起)→ 本机不可达列 BLOCKED
- 设计 33 服务端段所有 SV 验收(已在 2026-06-19 PASS,本任务不重测)
- UI 视觉:排行榜窗结算「已结算」提示 UI(设计 28 已声明延后,需美术)
- 跨物理设备真实双发场景(E2):依赖联网双端,自治模式以「同账号跨会话近似」、真跨设备列 BLOCKED

## 影响半径(本次改动 + 同步落点)

- 设计稿:`22-rank-system.md` 整段改写 §3.5 / §3.7 / §3.8 + 头部引子 / 立项 / §一表 3 行 / §二 mermaid / §3.1 末句 / §3.2 末句 / §3.3 首句 / §四 时序图 / §五 验收点(删 DUE/ST 组 + 加 RT 组)/ §六 O5/O9 / §七 风险表(删 3 加 3 改 1)。
- 同步:`33-rank-settle-server.md` 读前必看第 6 条 + §六 关系表 + §九 风险表 1 条;`28-rank-window-art.md` §七 处置表 2 行 + §十 D4 / D6 2 行;`32-mail-server.md` §五 / §九 各 1 处描述。
- 代码:`Assets/GameScripts/HotFix/GameLogic/Module/Rank/RankService.cs`(删 4 方法 / 1 字段 / 1 内部辅助 / `HasClaimable` 判定体简化)+ `RankPersistence.cs`(DTO 删 1 字段)+ `RankModel.cs`(删 `SettleResult` struct);测试 `Assets/Editor/Tests/BlockBlast/RankSystemTests.cs`(删 DUE/ST 整组 + RD 改 + P1 改)+ `RankWindowTests.cs`(删 `OpenDate` 注入行)。
- 入站锚审计已过:`#22-rank-system::settle` / `::due` / `::orchestrate` / `::reddot` / `::persist` / `::text` 全保留(33 / 28 / 32 等他篇链接不漂);可见章节号 §3.5 / §3.7 / §3.8 / §3.9 编号未移位(采「整段改写不删整段」方式保住可见编号)。
- 全栈三刀客户端收口:31 远程源接 RPC + 33 服务端结算 + 本任务客户端段(本地结算退役 + 红点交棒邮件)= 排行榜全栈特性完整闭环;再回头看设计 22 是「客户端排行榜数据逻辑层 + 服务器接缝」,已与全栈现状对齐。
