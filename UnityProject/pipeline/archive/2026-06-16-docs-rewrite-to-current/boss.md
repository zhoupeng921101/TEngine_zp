# Boss 关单总结:docs-rewrite-to-current 设计文档对齐现状(2026-06-16)

## 任务定义
按 conventions **规则6**(过时即重写或删除、不加注;历史归 git)把融合后过时的设计文档对齐现状。源起:用户新方向「文档同步只用重写或删除,不要加注」——取代上一轮 docs-tidy 的勘误注做法。

## 参与环节
plan-only(纯 design-docs 重写,无代码)。boss 直接验收。0 打回。

## 两阶段
1. **转注**:01/02/11/12/13 五篇 docs-tidy 勘误/状态注 → 现状重写/删除(部分前序中断会话已做,本轮核验 + 补 09/10)。
2. **09/10 整篇重写(用户拍板 B)**:09 脱离「在 08 之上加」叙事(08 已删)、独立陈述合成订单切片现状;10 脱离「变更计划/现状=InjectChance→变更」时态、把得分驱动元素生成作现状设计。消除 10·§2 把已删 InjectChance 当现状并指向已重写 09 的硬矛盾。

## 用户拍板(2026-06-16)
① 文档同步只重写或删除、不加注;② 过时内容的「为什么」纯靠 git/commit message(不留拍板记录节于正文);③ 09/10 选「彻底重写为现状」(非最小修矛盾)。

## 验收结论:PASS(boss 直验)
- 残留勘误/状态注:7 篇 grep 0 命中。
- 矛盾消除:`InjectChance`/`PityThreshold` 在 09/10 0 残留;无「08之上/变更前/删X改写为」历史框架或 diff 叙事。
- 锚点契约完整:6 个入站锚(09#merge/#risk2/#tradeoff、10#hook/#map/#type)逐一在目标篇 id 集合中,零断锚;可见章节号未移位(10 删「移除收集」②时把 §三 改写成「依赖的元素设施」现状占位、保 #remove/#removed/#keep/#misnomer + §四 编号)。
- 数值未篡改:EnergyStart 20 / EnergyCap 30 / ScorePerElement 200 等抽样未变(29 当现状引用)。
- nav.js 语法 OK(GROUPS 随 10 改名同步卡片文案,href/side 未动);无 archive 死链;09/10 无 BOM。

## 规则侧落地(主会话先行,本任务的依据)
- conventions **规则6**「过时内容重写或删除,不加注保留」+ 「收尾必做」自检项。
- pipeline-plan 红线加「维护 design-docs 遵规则6 + 同任务同步被过时的他篇」。
- plan 记忆:line 10/11 原「加映射 callout 冻结正文」做法改为指向规则6;新增 line 13「可见章节号亦是入站契约,整篇重写须保 id + 章节号或同步全部入站」。

## 判断点(安全默认推进,留复核)
1. 10「移除收集」②内容按规则6 移出正文(已下线→删节,历史归 git);要留收集 demo 历史应去 git/单开归档篇,不回退正文。
2. 09/10 元素产出分工(09=切片总览、10=生成模型细节)有概念重叠但未合并;若嫌 10·§三 与 09·§七 重复度高可后续精简(本轮保留护 §四 编号 + 29 引用)。

## 遗留 / 后续
- 工作树未提交(用户自行提交):本日 design-docs 全部改动(融合 01/29 + 转注 7 篇 + 09/10 重写 + archive 删除 + nav.js + style.css)+ 规则栈(conventions/pipeline-plan/memory)+ pipeline state。
- 规则栈新增规则6,后续可 /audit 增量核(基线指针在 audit-log.md)。
