# 状态:策划(plan)

> 开工先读本文件(角色职责在 `.claude/agents/pipeline-plan.md`,spawn 时自动注入)。每完成一步就更新这里。

## 当前任务:把 09、10 两篇设计文档整篇重写为现状设计稿(plan-only)

### 交接区(交 boss 验收)
plan-only,无代码改动。目标态:09/10 读起来即当前事实——09 独立陈述合成订单切片现状(不再以「08 之上加 / vs 08」为骨架),10 把得分驱动元素生成作现状设计陈述(不再「变更计划」时态、不再把已删的 InjectChance 当现状指向 09)。改动文件:`design-docs/09-merge-order-energy.html`、`design-docs/10-score-element-rm-collect.html`、`design-docs/assets/nav.js`(GROUPS 两行卡片文案)。

### 各篇重写了哪些节

**09 — 脱离「08」叙事(框架,非数值)**
| 节 | 改动 |
|---|---|
| 标题区 subtitle | 删「在收集 Demo 切片之上叠加」起句,改为直接陈述三系统循环。 |
| §一(`#why`) | 标题「这一切片在 08 之上加什么」→「切片构成」;删「08 收集切片跑通…它的终点是…本切片复用前半段替换终点」叙事 + 整张「08 vs 本切片」对比表;改为「底层是元素携带链路 + 本切片接三套系统」的现状表(单列「本切片做法」)。「收集计数语义裁决」callout → 「被清元素的去向」现状陈述。 |
| §二两处 | 「沿用 08 的 GameOver」×2(失败条件 bullet、§四·风险3 bullet)→「复用 `GameOverWindow`」。 |
| §七(`#hook`)、§八 | §七 GameOverWindow 行「沿用 08」删;§八 Out-of-Scope「沿用 08 切片边界」→「Demo 切片边界」。 |
| 不动 | 立项信息表、§二 SVG、§三全系统设计、§四风险方案、§五配置表、§六浪漫餐厅取舍表(竞品对比非 08 对比,合法保留)、§九风险表——数值/公式/SVG 全未动。 |

**10 — 脱离「变更计划」时态(框架,非数值)**
| 节 | 改动 |
|---|---|
| 标题/`<title>`/footer | 「得分驱动元素生成 + 移除收集」→「得分驱动元素生成」(移除收集是已完成的一次性下线,正文不再承载,历史归 git)。 |
| subtitle + 立项信息 | 删「两件互不相干同批交付的改动①②」「main 已做掉大半本篇补完」change-plan 框架;改为「合成订单切片元素产出由得分驱动」现状导语 + 现状立项表(类型=数值模型)。 |
| §一(`#why`) | 「这一批改什么」①②需求表 + 「为何同批」callout → 「模型一句话」现状概述。 |
| §2.1(`#cur`) | 「被替换的现状:固定概率注入」(把已删 `InjectChance` 当现状指向 09·§七 = 原硬矛盾)→ 「为何用得分驱动数量」现状理由(无 diff、无指向 09)。 |
| §2.2(`#model`) | 删「沿用 09 既有补牌节奏/改动面最小」「投放时机取舍 dev 按此实现/与 09 一致/本批不做」change-plan 措辞;保留流程条 + 队列机制(现状)。 |
| §2.3(`#map`) | 标题删「(核心审定点)」+ 「这是用户要审的取舍」句;映射公式/常量表/代入表**逐字未动**。 |
| §2.4(`#type`) | 「沿用 09 的需求拉动」→「由需求拉动决定」;「保底计数器随旧模型退役」diff callout →「不设保底计数器」现状陈述。 |
| §2.5(`#drop`) | 「被取代/退役的旧机制」diff 表(InjectChance/PityThreshold 删/改写清单)→ 「得分量与玩家可见分的隔离」现状(`clearScore` 内部驱动量不计入订单分,= 29·§3.3 现状)。 |
| §三(`#remove`/`#removed`/`#keep`/`#misnomer`) | 整个「需求②:移除收集 + 保留边界」(§3.1 main 已删清单 / §3.2 共享设施保留 / §3.3 正名映射)→ 「本模型依赖的元素设施」现状(§3.1 词汇表与表现 / §3.2 统计与携带链路 / §3.3 命名约定);SVG 保留,左节点「①本批新逻辑」+ 容器「保留(已正名)」等 diff 文案改现状。 |
| §四(`#hook`) | 标题「挂接点/dev 改动清单」→「挂接点(一次落子的元素流)」;时序 SVG 保留(去 `(新增)`/`(不动)` 两处 diff 标注);「文件→改动(删X增Y改写为)」表 → 「文件→承担的角色」现状表。 |
| §五(`#accept`) | 「需求①/需求②」批次小标题 → 「得分→元素映射」「设施完整+不回归」;去「删除收集后/①改注入后须重新通过」diff 措辞;**验收逐档值未动**。 |
| §六(`#decide`) | 「待拍板清单」→「可调旋钮」;删「用户点名要审/与 09 一致最小改动」;删「无硬冲突需停手(彻底移除收集不冲突)」callout(已完成下线的 change-plan)。 |
| §七(`#risk`) | 删「①触及勿动的注入规则/用户授权改动」diff 行;`BlockScoring` 风险行改现状口径;余行保留。 |
| 不动 | 所有数值/公式/SVG 几何/代入表/验收逐档值。 |

### 三条硬约束核验结果

**① 锚点保全(逐条 verify)** — 6 个入站锚全部存在且唯一:
- 09:`#hook`(09·§七,11 引)/`#tradeoff`(09·§六,11 引)/`#risk2`(09·§四·2,11 引)——id-count 各 1。另 09·§3.1=`#merge`、§3.3=`#energy` 被 11 引,未动。
- 10:`#map`(§2.3,09+11 引)/`#hook`(§四,09 引)/`#type`(§2.4,11 引)——id-count 各 1。
- 可见章节号未移位:10 仍 2.3=map/2.4=type/四=hook;09 仍 六=tradeoff/四+risk2/3.1=合成/3.3=体力——与 11/29/09 prose 的「10·§2.3」「10·§四」「09·§六」「09·§3.1」等可见引用全部对得上。
- 库内全量 `09·§N`/`10·§N` 可见引用清点(grep)逐条落对应 id;跨文件 `file.html#anchor` 全解析;09 intra-anchor(`#risk1`×3/`#risk2`/`#risk3`/`#tradeoff`×2)、10 intra-anchor(`#decide`)全解析;**0 断链、0 `archive/` 残链**。
- **额外收益**:10 原 §2.1 指向 `09·§七#hook` 的引用(硬矛盾源)已随重写消失——10 现 0 条出站锚指向 09 的 anchor。

**② 数值/公式不动(改写前后 grep 比对)** — 全部一致:
- 09 体力档:EnergyStart=20、EnergyCap=30、PlaceCost=1/块、ClearRefund=消除行列数、OrderReward.Energy=+8、Regen=+1/120s。合成:MaxLevel=3、Lv折算1/2/4。订单:ActiveOrders=2、OrderReward.Score=等级×数量×50、d(单)=数量×2^(等级-1)、DemoGoalOrders=5。元素生成:ScorePerElement=200、MinElementsPerClear=1、MaxElementsPerClear=4、MaxPendingElements=12。兜底:UndoCharges=3/局。
- 10:ScorePerElement=200、MinPerClear=1、MaxPerClear=4、MaxPendingElements=12;公式 clearScore=被清格数×10+被清行列数²×30(BlockScoring=clearedCells*10+lines*lines*30);代入表 110/280/500/780/1100+ 与 ⌈⌉ 计算逐行未动;验收逐档 0→0、110→1、280→2、500→3、780→4、2000→4 未动。
- 29 把这些当现状引用——只改框架/时态/叙事,数值零改。

**③ 不加注、历史归 git** — 未加任何勘误/状态/「(已删X见git)」注、未加拍板记录节。grep 跨两篇:`勘误|状态注|08之上|vs08|现状=|删X增Y|改写为|这一批|需求①②|本批新逻辑|InjectChance|PityThreshold|Collect*|收集|沿用08|从…改成|退役|被取代` 全 0 命中。

### 收尾自检
- `node --check assets/nav.js` 通过(动了 GROUPS 两行)。两篇无 BOM(`head -c3`=`3c 21 44`=`<!D`)。
- SVG open/close 平衡(09:2/2,10:2/2);10 tail(related/footer/script)完整;h2:8/h3:10(7+3 带 id 进 auto-TOC,§五 2 个无 id h3 不进 TOC,与原一致)。
- 语体(规则5):新写句去 `骑在`(改「构建在…之上」);两篇无 `钉死/焊死/绑死/打死/死在`。原文保留的「喂订单/淹没/刷爆」属既有游戏经济用语,非拟人比喻,未过度改。
- nav.js:09 卡片 desc 去「注:元素注入规则已被 10 改为得分驱动」diff 注,改「元素产出由得分驱动(详见 10)」;10 卡片 title 去「+移除收集」、tag 改「切片·数值模型」、desc 改现状。href/side 未改(文件名不动,避免动全部入站 href)。

### 交 boss 的判断点(范围决定,有安全默认,已按默认推进)
1. **10 的 ② 移除收集内容整体移出正文**:②(移除收集 demo + main 已删清单 + 正名映射)是一次性已完成下线,按 conventions 规则6「已下线→删除该节,历史归 git」处理;原 §三承载它的位置改写成「本模型依赖的元素设施」现状(保 `#remove/#removed/#keep/#misnomer` 四 id 不断 auto-TOC、保 §四 编号不移以护「10·§四」入站引用)。安全默认=规则6 合规、可逆;若 boss 想另存一份「收集 demo 历史」应去 git/单开归档篇,不回退正文。
2. **09/10 元素产出内容的分工**:09=切片总览(§一构成 + §七挂接提一句「走得分驱动映射,详见10·§四」),10=得分驱动模型细节(公式/类型/挂接时序)。两篇仍有「元素产出」概念重叠但分工清晰(总览 vs 细节),**未擅自合并**——若 boss 认为 10·§三「依赖设施」与 09·§七挂接表重复度过高,可考虑 10·§三 精简为一句「设施见 09·§七」(本轮保留是因 §三 需占位以护 §四 编号 + 29 把这些设施当现状)。

(上一任务:docs-tidy 把 7 篇勘误/状态注转重写,plan-only,本会话承接其遗留的 09/10 框架级重写。)
