# plan 状态

> 开工先读本文件 + boss 交接区(角色职责在 `.claude/agents/pipeline-plan.md`,spawn 时自动注入)。每完成一项就更新这里。

> 上一个占用本槽的任务 `gameplay-no-rounds-infinite` 已暂停未进 dev,其设计基线已提交 `19303cdc`(设计 49 + 反向引用),恢复时从 dev 起、基线 = 已提交 design-docs,不依赖本状态文件。本槽现归在跑任务 `block-skin-switch`。

## 当前任务

**`block-skin-switch` 方块皮肤切换(plan→dev→test,client)** — plan 段完成,交 dev。

### 设计稿

- 新增 `design-docs/50-block-skin-switch.md`(已注册 nav.js GROUPS「切片设计」组 + index.html nav.js `?v=15`)。
- 设计意图全 code-free:皮肤状态模型(彩色态 / 单色态 + 当前单色标识)、状态机(初始彩色 → 首次全清转单色 → 每次全清换色防连续重复 → 单色不退回彩色)、单色随机选取规则(从真实存在 sprite 编号集随机、防相邻重复、退化兜底)、续存要求(局内态、缺字段保底、非法标识兜底)、美术规格(源目录 / 图集名 / sprite 命名)、功能点三档、待拍板 3 项、风险表。

### 关键设计决策(已按三段阶梯取默认,记此供 dev/boss 复核)

1. **「全清」口径**:本功能触发事件 = **棋盘整盘被清空(全清 / All-clear / perfect clear)**,即设计 11·§5.4/§5.5 的全清判定。**≠ DDA「清屏窗口」**(01/02/07 的分数难度窗,语义不同)。设计稿 §intro 用对照表硬区分,全文用「全清」不用「清屏」。
2. **皮肤切换跟随哪个全清**(待拍板 #1,默认):与女神好评 / 图案奖励**同口径**(全清「已武装」状态触发),复用既有全清事件单一口径,不另立第二份「什么算全清」定义。备选 = 每次整盘清空都换(含连续第二次全清)。行为差异仅在罕见「连续两次全清」局面。
3. **单色 = 全盘统一一张**(不分方块类型),非按类型换一套配色,非整套换风格(用户拍板)。
4. **候选池 = 真实存在 sprite 编号集**(blocks_skin 编号不连续:7-25 / 29-47 / 51-97 / 226-477),**不是 1..N 连续区间**(否则选到留空编号 → 空图)。这是给 dev 的硬约束。
5. **皮肤态归局内态续存**(同「全清武装位」类),对齐设计 14 局内段 + 无尽模型 49;缺字段保底 = 彩色态。

### 验收标准位置

`design-docs/50-block-skin-switch.md` §八(`#50-block-skin-switch::accept`),已分两组:

- **A 组 · 可单测**(A1–A9):状态机(初始彩色 / 首次转单色 / 后续换色 / 不退彩色)、防相邻重复、候选池域正确(不含留空编号)、续存往返保真、缺字段+非法值保底、退化兜底、皮肤切换不污染全清结算。test 主力。
- **B 组 · 需运行手验**(B1–B4):全新彩色渲染 / 全清后整盘单色 / 再全清换不同单色 / 退出重进保持。运行环境不可达判 BLOCKED 不判 FAIL(A 组已覆盖可单测逻辑)。

### 反向引用同步(plan 已在本任务内完成,dev 无需再补文档)

| 篇 / 节 | 改了什么 | 状态 |
| --- | --- | --- |
| `11-core-loop-completion.md` §5.4 | 全清奖励表后加 NOTE:全清还触发方块换皮(纯视觉第三轴,不发经济奖励、不进结算),指向设计 50 | ✅ 已同步 |
| `01-gameplay-overview.md` §三方块库 | 加一条「方块外观分两套皮肤(形状之上的渲染层)」,指向设计 50 | ✅ 已同步 |
| `14-save-system.md` §3.1 两张表 | 局内段加「方块皮肤态(彩色/单色 + 当前单色标识)」行(进盘 + 缺字段保底为彩色态),指向设计 50 | ✅ 已同步 |
| `49-infinite-no-rounds.md` | 局内态续存边界**已显式委托设计 14**(「存档边界见设计 14」),14 已更新作单一清单源 → 49 无需逐项改 | ✅ 无需改(委托) |
| `02 / 07`「清屏窗口」 | 是 DDA 分数窗(另一语义),与本功能无关 | ✅ 无需改 |

> 反向引用只动设计层(forward cross-ref + 存档清单补行),无代码符号。11 / 01 的既有正文未过时(全清奖励表 / 方块库形状仍正确),加的是 forward NOTE 非勘误注,合 conventions §6。

### 给 dev 的调查点(plan code-blind,dev 现场推导接缝)

1. **方块当前如何按类型取 sprite**:彩色态现状渲染——每种方块类型取各自固定彩色图(图集 `Atlas_blocks_blocks_main`,sprite 名 `blocks_main_<编号>`,8 张:1/6/14/15/16/18/19/21)。dev 定位棋盘格 / 落子方块的 sprite 赋值接缝,皮肤态在此分叉:彩色态走现状按类型取图,单色态全盘统一取「当前单色标识」那张(图集 `Atlas_blocks_blocks_skin`,sprite 名 `blocks_skin_atlas_<编号>`,337 张)。
2. **全清事件的代码触发点**:整盘被清空判定 —— 复用设计 11·§5.5 全清结算流水线里的全清判定 / 全清武装位逻辑(默认口径:已武装全清触发时切皮肤,见决策 #2)。dev 定位该判定接缝,在其上挂皮肤切换(纯附加,不改结算)。
3. **局内态续存走哪条存档通道**:皮肤态(皮肤模式 + 当前单色标识)进局内态存档段,与「全清武装位 / 连消链长」同通道(设计 14 局内段)。dev 定位整盘序列化 / 反序列化的局内态导出 / 导入接缝,加这两个字段 + 缺字段保底(缺 = 彩色态)+ 加载时单色标识非法校验(指向候选池外 → 默认单色态重随机,见验收 A6)。
4. **单色资源候选池构造**:dev 取 `Atlas_blocks_blocks_skin` 图集实际存在的全部 sprite 名 / 编号集(README 在 `Assets/AssetRaw/UIRaw/Atlas/blocks/blocks_skin/`,列编号段),候选池 = 该真实集,不是连续区间。可选项:44×44 小尺寸 6 张(51/52/53/54/93/247)是否纳入,默认纳入(待拍板 #2)。

### 待拍板(均有安全默认,不入 blockers;boss 关单复核)

设计稿 §九集中列出 3 项:① 皮肤切换全清口径(默认同女神口径)② 44×44 小图是否纳入候选池(默认纳入)③ 非法标识兜底方向(默认单色态重随机)。三项均可逆,默认即可推进。

### 自检(conventions 收尾)

- [x] 语体扫描:`钉死/焊死/绑死/打死/死在` 在本次改/新增 .md 零命中。
- [x] design-docs 与代码解耦:设计稿正文无代码符号 / 路径 / file:line;美术规格(源目录 / 图集名 / sprite 命名)属设计输入非代码定位;代码层接缝定位仅在本交接区给 dev,稿内不写。
- [x] 过时他篇同步:11 / 01 / 14 forward cross-ref + 14 存档清单补行均同任务内完成,无勘误注叠旧;既有正文未过时(加的是补充非覆盖)。
- [x] 工作态可识别所属任务(本槽归 block-skin-switch;上一暂停任务基线已提交,本文件顶注说明)。
- [x] UI 投放边界:本功能纯棋盘渲染行为(无新窗口),验收 A 组锚在可单测状态机 / 续存逻辑,B 组运行手验不可达判 BLOCKED 不阻断。
