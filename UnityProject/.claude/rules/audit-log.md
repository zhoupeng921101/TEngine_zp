# 规则审计基线指针(注入态最小常驻)

> 本文件每窗口注入,只留**最近审计基线指针**,供 `/audit` 增量短路 + 手动审计的最小提醒。完整审计叙述、改动登记、历史 → `.claude/rules-archive/audit-log.md`(活账本,注入路径外,/audit 时 Read)。新审计:更新本基线指针 + 把叙述写进活账本,不在此处堆叙述。

最近审计:**第二十次** @ 增量基线 = 第十九次落账 commit `e90a6dc2`(第十九次叙述未提交,与本次合并落账)。本会话两路重大改动:① `pipeline-auto` 自治 7 刀 backlog 推进(Tier 2 全栈 + Tier 4 Cumulative 节律全栈,提交 `c861d610`/`13a99081`/`eff3376b`/`3825e1d1`/`750ec980`/`3d2e966c`/`29597b26`/`b1fdec0c`/`1f18d35b`/`ffc88f68`/`be2b3b51`/`715d66ee`,经 boss 关单事务归档到 `pipeline/archive/2026-06-21-*` 七个目录 + CLOSE.md);② **memory 信道统一**:`pipeline/memory/` 整目录退役,所有角色经验迁到 `.claude/agent-memory/pipeline-<role>/`(legacy-bullets.md 整体归档 + 既有结构化文件并存),改 7 个 agent 卡片(14 处引用)+ pipeline SKILL.md(L17 boss 注入路径 + L36 spawn 简报指令)+ pipeline-server-test-codex.md(Codex 启动器 memory 路径)。**根因**:agent-memory 是 Claude Code 系统级标准注入(frontmatter `memory: project` 字段触发),pipeline/memory 是手动 Read,两套并存导致双源漂移(本会话已发现 BLOCKED-vs-FAIL 同时存在两边)。七项查全 clean:重复(legacy-bullets 与既有独立 .md 文件去重 1 条 server-test 的 internal-trigger;指针 MEMORY.md 链接 legacy-bullets 唯一源,无副本)、死规则(无,迁移仅信道变化,规则内容不变)、矛盾(三态契约/agent 卡片不变)、信道(系统自动注入路径优于手动 Read,统一后不再混合)、净增(规则栈净减,删 8 个 pipeline/memory 文件 + 7 备份,新增 7 个 legacy-bullets.md;净结构更清晰)、孤儿(无,所有迁移文件均有 MEMORY.md 索引指)、#7 可执行性(agent 卡片新指令逻辑自洽:系统注入自动+手动 Read 状态文件,两路职责清晰)。**carry-forward 新增**:legacy-bullets.md 是过渡形态(bullet list 非结构化),长期目标按 conventions §6 演化式重写:agent 触发某 bullet 内容时优先独立化为 feedback-/project- 文件 + 删 legacy-bullets 对应行,**避免规则化「全部独立化」批任务**(违 conventions §3 准入·一次性事故 vs 复发性,大批改动收益不抵成本)。**并发**:工作树仍有非本会话未跟踪件(`../.codex/`、`../Fantasy/`、`tmpiyltpgv7.webp` 等,来源未知 flag 用户),本次迁移落账未提交、有并发清理风险,尽快提交。下次增量以本落账提交后 commit 为基线。详见活账本第二十次段。
