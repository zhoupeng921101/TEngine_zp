# 规则审计基线指针(注入态最小常驻)

> 本文件每窗口注入,只留**最近审计基线指针**,供 `/audit` 增量短路 + 手动审计的最小提醒。完整审计叙述、改动登记、历史 → `.claude/rules-archive/audit-log.md`(活账本,注入路径外,/audit 时 Read)。新审计:更新本基线指针 + 把叙述写进活账本,不在此处堆叙述。

最近审计:**第十五次** @ 增量基线 = 第十四次审至 commit `f36a96a1`,审至本会话已提交 `34692918`。本会话 design-docs SVG→mermaid + ①②③ 杠杆重构(`59c53699`/`3a2c6474`/`ce417548`/`0521b0dc`/`4766ba67`)属文档正文、非规则栈不入范围;规则栈实改两笔:`6834c1d9`(并行会话:失败模式纵深贯穿四角色 + 需求降层 + 范围三档 + 熔断根因诊断 + 图表无条件改条件触发)、`34692918`(本会话:plan 卡文档表现对齐 design-docs 现状——强调 `<b>`→`**`、callout→GFM `> [!…]`、简单表→md 表、新增 GFM 块空行规约)。结论:无重复(崩法四角色有意切分,同三段阶梯并列模式)、无矛盾(旧「CJK `**` 不可靠」经 marked v15 实测证伪[仅紧贴「」标点失效]后覆盖式收窄,callout/表格新条款与 nav.js 钩子 `0521b0dc` 一致,grep 无他篇引用旧约定)、无死规则/孤儿;#4 信道无增减、#5 注入态 rules/ 不变。carry-forward:plan「向上对体验」自检仍无触发实例;**新增监视**——后续设计文档若仍用旧 `<b>`/`<div class="callout">` 简单框风格,即 plan 卡同步未传导。下次增量以 `34692918` 为基线。详见活账本第十五次段。
